import { Card, CardContent, CardHeader } from '@material-ui/core'

export const FeedbackCard = ({ note }) => {
  return (
    <div>
      <Card>
        <CardHeader />
        <CardContent></CardContent>
      </Card>
    </div>
  )
}
